{
  "name": "webseal",
  "version": "1.0.0",
  "description": "Professional webpage snapshot and watermarking tool",
  "main": "index.html",
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2024-01-01",
    "deploy": "wrangler pages deploy . --project-name=webseal",
    "preview": "wrangler pages deploy . --project-name=webseal --env=preview"
  },
  "keywords": [
    "webpage",
    "screenshot",
    "watermark",
    "evidence",
    "cloudflare"
  ],
  "author": "WebSeal Team",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}